// highTreeからlowTreeを生成する

import { Toplevel } from "./parse.js";

export type LoweringState = {};

export type L_TopLevel = {};

export function lowering(hTree: Toplevel[]): L_TopLevel[] {
    const state: LoweringState = {};
    return [];
}
